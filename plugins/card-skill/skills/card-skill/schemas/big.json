{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "card-skill big mode input",
  "description": "Structured input for big mode CLI rendering. Big mode renders a large-text poster (1080x1440) with optional ghost watermark character.",
  "type": "object",
  "additionalProperties": false,
  "required": ["mode", "phrase"],
  "properties": {
    "mode": { "type": "string", "const": "big" },
    "design": {
      "type": "string",
      "description": "Design system name. Defaults to 'vercel'. See scripts/lib/designs.js for full list.",
      "default": "vercel"
    },
    "tone": { "type": "string", "enum": ["reflective", "sharp", "warm", "technical"], "description": "Default-selection tone. Ignored when design is explicit." },
    "phrase": {
      "type": "string",
      "description": "Main body text. Use <br> for line breaks and <span class=\"accent\">word</span> to highlight words. Example: '<span class=\"accent\">Agent</span> 不是<br>加了循环'",
      "minLength": 1
    },
    "accent_words": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Words from phrase to wrap in <span class=\"accent\">. Only used when phrase doesn't already contain accent spans. Example: ['Agent']"
    },
    "ghost_char": {
      "type": "string",
      "description": "Single character used as large low-opacity watermark. Auto-derived from first accent_word or phrase character if not provided.",
      "default": ""
    },
    "font_size": {
      "type": "number",
      "description": "Override auto-calculated font size (px). Auto-calc uses ≤10 chars→220px, 11-20→190px, 21+→160px.",
      "minimum": 100,
      "maximum": 300
    },
    "attribution": {
      "type": "string",
      "description": "Optional attribution line below the phrase. Example: '— Alan Kay'",
      "default": ""
    },
    "logo": {
      "type": "string",
      "maxLength": 1024,
      "pattern": "^(?:[A-Za-z]:[\\\\/]|/(?![/?\\\\]))",
      "description": "Optional path to a brand logo image. Omit for an unbranded output."
    },
    "brand_name": {
      "type": "string",
      "description": "Optional brand name in the colophon footer. Omit for an unbranded output."
    }
  },
  "examples": [
    {
      "mode": "big",
      "design": "linear",
      "phrase": "Agent 不是<br>加了循环",
      "accent_words": ["Agent"],
      "ghost_char": "A",
      "attribution": "—— 某人"
    }
  ]
}
