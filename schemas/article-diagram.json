{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "card-skill article-diagram mode input",
  "description": "Structured input for in-article compression formula cards. The default shape is a compression pack: formula, one sentence, and a supporting structure map. Legacy concept-map, process-flow, and boundary-model inputs are still accepted for compatibility. Visible text should follow the source article or requested language unless the user explicitly asks for translation or bilingual output.",
  "type": "object",
  "additionalProperties": false,
  "required": ["mode", "title"],
  "oneOf": [
    {
      "required": ["formula", "sentence", "structure"],
      "not": {
        "anyOf": [
          { "required": ["family"] },
          { "required": ["logo"] }
        ]
      }
    },
    {
      "required": ["family", "nodes"],
      "not": {
        "anyOf": [
          { "required": ["formula"] },
          { "required": ["sentence"] },
          { "required": ["structure"] },
          { "required": ["render_plan"] }
        ]
      }
    }
  ],
  "properties": {
    "mode": { "type": "string", "const": "article-diagram" },
    "family": {
      "type": "string",
      "enum": ["concept-map", "process-flow", "boundary-model"],
      "description": "Diagram family. Pick the layout before writing labels."
    },
    "aspect": {
      "type": "string",
      "enum": ["body-2-1", "body-3-2", "body-4-3"],
      "description": "Canvas ratio for article body placement. Compression formula cards default to body-2-1 and expand to body-3-2 when dense; legacy diagrams default to body-3-2."
    },
    "design": {
      "type": "string",
      "description": "Optional explicit compatibility palette name. Changes colors only; the default uses the technical house tone."
    },
    "tone": { "type": "string", "enum": ["reflective", "sharp", "warm", "technical"], "description": "House color tone. Changes color tokens only and is ignored when design is explicit." },
    "title": {
      "type": "string",
      "description": "Short diagram title. It should name the relationship, not the output format. Use the source or requested language for visible text.",
      "minLength": 1
    },
    "subtitle": {
      "type": "string",
      "description": "Optional short context line. Use the source or requested language for visible text."
    },
    "formula": {
      "type": "string",
      "description": "Compression-pack formula. Express the core relation, invariant, or transformation in compact visible language."
    },
    "sentence": {
      "type": "string",
      "description": "Compression-pack one-sentence takeaway. This is the human-readable theorem."
    },
    "render_plan": {
      "type": "string",
      "enum": ["auto", "summary", "structure", "split"],
      "description": "Compression-pack render plan. auto defaults to a formula card containing formula and sentence only. Use structure or split only when explicitly producing the supporting structure view."
    },
    "structure": {
      "type": "object",
      "description": "Compression-pack structure map that supports the formula and sentence.",
      "required": ["nodes"],
      "properties": {
        "nodes": {
          "type": "array",
          "minItems": 2,
          "maxItems": 6,
          "items": {
            "type": "object",
            "required": ["id", "label"],
            "properties": {
              "id": { "type": "string" },
              "label": {
                "type": "string",
                "description": "Visible structure node label. Use the source or requested language."
              },
              "note": {
                "type": "string",
                "description": "Optional visible node note. Keep it shorter than the label."
              }
            }
          }
        },
        "relations": {
          "type": "array",
          "maxItems": 6,
          "items": {
            "type": "object",
            "required": ["from", "to"],
            "properties": {
              "from": { "type": "string" },
              "to": { "type": "string" },
              "label": {
                "type": "string",
                "description": "Optional visible relationship label. Use it only when it changes interpretation."
              }
            }
          }
        }
      }
    },
    "nodes": {
      "type": "array",
      "description": "Legacy family visible concepts, steps, or entities. Keep labels short and in the source or requested language.",
      "minItems": 2,
      "maxItems": 6,
      "items": {
        "type": "object",
        "required": ["id", "label"],
        "properties": {
          "id": { "type": "string" },
          "label": {
            "type": "string",
            "description": "Visible node label. Use the source or requested language."
          },
          "note": {
            "type": "string",
            "description": "Optional visible node note. Use the source or requested language."
          },
          "zone": {
            "type": "string",
            "description": "Required for boundary-model nodes; must match a zone id."
          }
        }
      }
    },
    "links": {
      "type": "array",
      "description": "Optional relationships between nodes. Links must point to known node ids.",
      "maxItems": 6,
      "items": {
        "type": "object",
        "required": ["from", "to"],
        "properties": {
          "from": { "type": "string" },
          "to": { "type": "string" },
          "label": {
            "type": "string",
            "description": "Optional visible relationship label. Use the source or requested language."
          },
          "direction": {
            "type": "string",
            "enum": ["one-way", "two-way", "none"]
          }
        }
      }
    },
    "zones": {
      "type": "array",
      "description": "Required for boundary-model. Zones render as fixed nested regions.",
      "minItems": 2,
      "maxItems": 4,
      "items": {
        "type": "object",
        "required": ["id", "label"],
        "properties": {
          "id": { "type": "string" },
          "label": {
            "type": "string",
            "description": "Visible zone label. Use the source or requested language."
          },
          "description": {
            "type": "string",
            "description": "Optional visible zone description. Use the source or requested language."
          }
        }
      }
    },
    "caption": {
      "type": "string",
      "description": "Optional one-sentence interpretation. Keep it short and in the source or requested language."
    },
    "source": {
      "type": "string",
      "description": "Optional source line in the colophon."
    },
    "logo": {
      "type": "string",
      "maxLength": 1024,
      "pattern": "^(?:[A-Za-z]:[\\\\/]|/(?![/?\\\\]))",
      "description": "Optional path to a brand logo or avatar image. Omit for an unbranded output."
    },
    "brand_name": {
      "type": "string",
      "description": "Optional brand or author name in the colophon footer. Omit for an unbranded output."
    }
  },
  "examples": [
    {
      "mode": "article-diagram",
      "title": "Safety lives at the boundary",
      "formula": "Safety = single writer + shared trace + bounded tools",
      "sentence": "Multi-agent work stays safe when only one execution line writes while helpers contribute judgment through a shared trace.",
      "structure": {
        "nodes": [
          { "id": "trace", "label": "Shared trace", "note": "Decision history" },
          { "id": "writer", "label": "Single writer", "note": "Only mutation path" },
          { "id": "helpers", "label": "Helper agents", "note": "Explore and verify" },
          { "id": "tools", "label": "Bounded tools", "note": "Controlled action" }
        ],
        "relations": [
          { "from": "helpers", "to": "trace", "label": "contribute" },
          { "from": "trace", "to": "writer", "label": "guides" },
          { "from": "writer", "to": "tools", "label": "executes" }
        ]
      },
      "caption": "The structure map explains why parallel intelligence still needs a single write boundary."
    }
  ]
}
